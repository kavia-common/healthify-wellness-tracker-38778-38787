//
// Ocean Professional Theme Tokens for Healthify
//

// PUBLIC_INTERFACE
export const tokens = {
  /** Primary blue used for main interactive elements and highlights. */
  primary: '#2563EB',
  /** Amber accent used for success badges and retro accents. */
  secondary: '#F59E0B',
  /** Success color aligned with secondary amber for cohesive theme. */
  success: '#F59E0B',
  /** Error red for destructive actions and alerts. */
  error: '#EF4444',
  /** Default page background for light mode. */
  background: '#f9fafb',
  /** Elevated surfaces like cards and toolbars. */
  surface: '#ffffff',
  /** Base text color. */
  text: '#111827',
  /** Subtle ocean gradient background used across app. */
  gradient: 'linear-gradient(180deg, rgba(59,130,246,0.10), #f9fafb)',
};

export default tokens;
